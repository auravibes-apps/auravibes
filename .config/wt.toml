# Worktrunk configuration
# https://worktrunk.dev/

[[pre-start]]
# Copy selected gitignored caches from the primary worktree before setup.
copy = "wt step copy-ignored"

[[pre-start]]
# Install the FVM Flutter version configured in .fvmrc
fvm = "fvm use"

[aliases]
# Start or stop only the Docker project derived for the current worktree.
server-start = "./scripts/start_server.sh"
server-stop = "./scripts/stop_server.sh"

[pre-remove]
# Worktree removal must not leave its isolated database containers running.
server-stop = "./scripts/stop_server.sh"

# Local CI: runs after rebase and before the target branch advances.
[[pre-merge]]
quick = "fvm dart run melos run validate:quick"

[[pre-merge]]
test = "fvm dart run melos run test:local --no-select"

[[pre-merge]]
dependencies = "fvm dart run dependency_validator"

[[pre-merge]]
imports = "fvm dart run import_sorter:main --exit-if-changed"
