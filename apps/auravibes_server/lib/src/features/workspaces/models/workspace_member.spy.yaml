class: WorkspaceMember
table: workspace_member
fields:
  workspaceId: int
  userId: String
  role: String
  createdAt: DateTime
  removedAt: DateTime?
indexes:
  workspace_member_workspace_user_idx:
    fields: workspaceId, userId
    unique: true
