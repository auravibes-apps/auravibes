class: WorkspaceSecret
table: workspace_secret
fields:
  workspaceId: int
  secretKind: WorkspaceSecretKind
  scope: WorkspaceSecretScope
  ownerUserId: String
  resourceId: String
  ciphertext: ByteData
  nonce: ByteData
  authenticationTag: ByteData
  algorithm: String
  keyVersion: int
  displaySuffix: String?
  revision: int
  createdAt: DateTime
  updatedAt: DateTime
  deletedAt: DateTime?
indexes:
  workspace_secret_identity_idx:
    fields: workspaceId, secretKind, scope, ownerUserId, resourceId
    unique: true
